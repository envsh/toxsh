TODO
    改进，输出快的时候经常断开连接，因为包损坏Packet Corrupt。
        可用类似TCP的包seq方式初步验证。
    优化pull下行由于webserver的输出buffer所需要的填充字节带来的额外带宽开销。
    优化，合并从sshd或者ssh命令行读取包，读取到一定字节数或者时间超时再push到comet服务器。
    新功能，加入多会话连接支持，目前只支持一个连接。
    优化，发送包从十六进制转换为62进制编译，减小包长度。
